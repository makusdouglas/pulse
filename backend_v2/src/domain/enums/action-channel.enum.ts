export enum ActionChannel {
  WHATSAPP = 'whatsapp',
  EMAIL = 'email',
  PHONE = 'phone',
  IN_PERSON = 'in_person',
  OTHER = 'other',
}

export enum ActionResult {
  DELIVERED = 'delivered',
  READ = 'read',
  REPLIED = 'replied',
  FAILED = 'failed',
  PENDING = 'pending',
}
